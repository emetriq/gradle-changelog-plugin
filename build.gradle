group = 'com.emetriq.gradle'

version = '0.0.2'

apply plugin: 'groovy'
apply plugin: 'maven'

repositories{
    jcenter()
}

dependencies {
    compile gradleApi()
    compile 'com.netflix.nebula:nebula-release-plugin:4.0.1'
}

